name: hrms_qreams
description: "HRMS Qreams - Human Resource Management System"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ Add these commonly used dependencies
  provider: ^6.1.2
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7
  shared_preferences: ^2.3.2
  http: ^1.2.2
  geolocator: ^13.0.1
  geofencing_flutter_plugin: ^1.7.1
  geofencing_api: ^2.0.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.0
  flutter_background_service: ^5.0.5
  # Added By uday on 30_10_2025: Request runtime notification permission on Android 13+
  permission_handler: ^11.3.1
  # Added By uday on 30_10_2025: Open OEM-specific settings pages (Xiaomi AutoStart, Battery Optimization)
  android_intent_plus: ^5.1.0
  # Added By uday on 30_10_2025: Get package name for intents
  package_info_plus: ^8.0.2
  # Added By uday on 30_10_2025: Firebase core + messaging
  firebase_core: ^3.5.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^17.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/company_logo.jpeg
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
